<!--Pages/wxs/wxs.wxml-->

<!-- 1.wxs的使用方式 -->
<!-- <wxs module="info">
  //JS代码
  var message = "Hello world";
  var age = 18;

  var sum = function(num1, num2){
    return num1 + num2;
  }

  module.exports = {
    message: message,
    age : age,
    sum: sum
  }
</wxs> -->

<!-- 2.wxs 的第二种方式-->
<!-- 定义在单独的wxs文件中的内容，必须进行wxs标签的导入才能使用 -->
<!-- 不能使用绝对路径、只能使用相对路径 -->
<wxs src="../../wxs/info.wxs" module="info"/>

<view>{{info.message}}</view>
<view>{{info.age}}</view>
<view>{{info.sum(20, 30)}}</view>

<!-- 3.wxs的应用 -->
<!-- 对price保留两位小数 -->
<wxs src="../../wxs/format.wxs" module="format"/>
<view>{{format.priceFormat(price, 3)}}</view>